
<%= render 'form' %>

 <% @messages.each do |message| %>
  <table>
    <tbody>
      <tr>
        <td><%= message.title %><th>user_name</th></td>
      </tr>
      <tr>
        <td><%= image_tag(message.image_url, class: 'list_image') %>
      </tr>
      <tr>
        <td><%= message.message %></td>
      </tr>
      <tr>
        <td><%= link_to '詳細', message %></td>
        <td><%= link_to '修正', edit_message_path(message) %></td>
        <td><%= link_to '削除', message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    </tbody>
  </table>
      <hr>
<% end %>

<%= link_to 'New Message', new_message_path %>
