<h1>Welcome UPARTNER</h1>

<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<%= render 'form' %>

<div id="user_new">初めての方は、以下より、新規会員登録をしてください</div>
<%= button_to '新規会員登録', new_user_path, :method => :get %>

