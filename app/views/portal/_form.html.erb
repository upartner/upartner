#encoding: utf-8
<%= form_for(@user,:url => portal_login_path) do |f| %>
  <div class="field user_id">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>
  <div class="field password">
    <%= f.label :password %>
    <%= f.password_field :password %>
  </div>
  <div class="actions login_btn">
    <%= f.submit 'ログイン' %>
  </div>
<% end %>
